/**
 * MoneyNaija Mobile Application Configuration
 */

// Toggle this flag for sandbox mock flow during frontend development/testing
export const DEV_AUTH_MOCK = true;

// Default API Base URL targeting the sandbox backend (e.g. Android Emulator localhost loopback)
export const DEFAULT_BASE_URL = 'http://10.0.2.2:3000';
